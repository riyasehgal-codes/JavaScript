let object={
    items :"pen",
    price:100,
};
console.log("The cost of",object.items,"is",object.price);
// this will be same as 

let output= `the cost of ${object.items} is ${object.price}`;
console.log(output);